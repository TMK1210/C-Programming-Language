#include <stdio.h>

int main() {
    char c; 

    printf("Enter a sentence: \n");

    while ((c = getchar()) != EOF) {
        if (c == ' ' || c == '\t' || c == '\n') {
                putchar('\n');  
        } 
        else {
            putchar(c);
        }
    }

    return 0;
}
